import React from 'react';
import { SocialIcon } from 'react-social-icons';

export const TwitterIcon = ({ url, onClick }) => (
  <SocialIcon network="twitter" url={url} onClick={onClick} />
);
export const FacebookIcon = ({ url, onClick }) => (
  <SocialIcon network="facebook" url={url} onClick={onClick} />
);
export const GoogleIcon = ({ url, onClick }) => (
  <SocialIcon network="google" url={url} onClick={onClick} />
);
export const PinterestIcon = ({ url, onClick }) => (
  <SocialIcon network="pinterest" url={url} onClick={onClick} />
);
export const InstagramIcon = ({ url, onClick }) => (
  <SocialIcon network="instagram" url={url} onClick={onClick} />
);
export const LinkedinIcon = ({ url, onClick }) => (
  <SocialIcon network="linkedin" url={url} onClick={onClick} />
);
export const WhatsappIcon = ({ url, onClick }) => (
  <SocialIcon network="whatsapp" url={url} onClick={onClick} />
);
